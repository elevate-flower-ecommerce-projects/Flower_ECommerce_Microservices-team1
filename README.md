# Flower_ECommerce_Microservices-team1

## Architecture

The solution follows Vertical Slice Architecture: each feature owns its endpoint/controller, application logic, DTOs, validation, and other feature-specific components within its own `Features` folder. New features must follow this organization and should not be added to centralized controller folders.

